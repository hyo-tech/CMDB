# NATS Server Configuration
port: 4222

# Monitor endpoint for health checks
http_port: 8222

# Maximum payload size (16MB) - CBOR serialized CachedMaps and export files can exceed the previous 1MB limit
max_payload: 16777216

# JetStream configuration
jetstream {
  store_dir: "/data"
}

